#include "pch.h"
#include <iostream>

using std::cout;
using std::cin;

int main()
{
	int a = 0, b = 0;
	setlocale(LC_ALL, "rus");

	cout « "Меня зовут Иван Лунин\nМне 17 лет\nЯлюблю слушать музыку и смотреть фильмы\n" « char(234) « std::endl;

	cout « "\nEnter coefficient: ";
	cin » a;
	cout « "Enter term: ";
	cin » b;

	if a == 0
	{
		cout « "error\n";

		getchar();
		return(0);
	}

	cout « a « "*x+" « b « "=0" « std::endl;
	cout « a « "*x=" « - b « std::endl;
	cout « "x=" « - b « "/" « a « std::endl;
	cout « "x=" «(-b) / a « std::endl;

	getchar();
	return 0;
}
